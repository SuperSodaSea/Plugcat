package build.hdmioutexample

import mill._
import scalalib._

object `package` extends build.Module {
    def moduleDeps = Seq(build.hdmi)

    def mvnDeps = Seq(
        mvn"org.chipsalliance::chisel:7.0.0",
    )
    def scalacPluginMvnDeps = Seq(
        mvn"org.chipsalliance:::chisel-plugin:7.0.0",
    )
}
